import { keyframes } from '@emotion/react';
import styled from '@emotion/styled';

export const Container = styled.div`
       padding: 10px;
`;

export const PageContainer = styled.div`
       max-width: 800px;
       margin:auto;
       font-size: 15px;

       h1, h2, h3 {              
              text-shadow: 0px 1px 2px #ffa123;
       }
`;